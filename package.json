{
  "name": "gerpain-monorepo",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter gerpain dev",
    "build": "bun run --filter gerpain build",
    "test": "bun run --filter '@gerpain/*' test && bun run --filter gerpain test",
    "typecheck": "bun run --filter '@gerpain/*' typecheck && bun run --filter gerpain typecheck"
  }
}
